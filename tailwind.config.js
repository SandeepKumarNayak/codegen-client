/** @type {import('tailwindcss').Config} */
// Tailwind v4 — config via CSS. This file kept for tooling compat.
export default {
    content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
    darkMode: 'class',
    theme: { extend: {} },
    plugins: [],
}
